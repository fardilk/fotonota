class CameraService {
  // Placeholder for camera / image picking / OCR logic
  Future<String> takePicture() async {
    // TODO: integrate camera plugin
    await Future.delayed(const Duration(milliseconds: 300));
    return 'path/to/mock.jpg';
  }
}
